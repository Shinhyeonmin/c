#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void)
{
    //팰린드롬 수
    /*char palin[6];

    while (scanf("%s", palin)&&palin[0]!='0')
    {
            int num = strlen(palin);
            int test = 0;
            for (int i = 0; i < num / 2; i++)
            {
                if (palin[i] != palin[num - i - 1])
                {
                    test = 1;
                    break;
                }
            }

            if (test == 1)
                printf("no\n");
            else
                printf("yes\n");
    }*/

    //구조체

    return 0;
}
