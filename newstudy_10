#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <ctype.h>
#include <stdlib.h>


int main(void)
{
    //scanf("%s",d); ==>%s는 문자열에 넣을 때 사용

    // 소문자를 대문자로 바꿔라
    /*char arr[50];
    scanf("%s",arr);

    for (int i = 0; arr[i]!='\0'; i++)
    {
        if (islower(arr[i]))
            arr[i] = toupper(arr[i]);
    }

    for (int i = 0; arr[i]!=0; i++)
        printf("%c", arr[i]);*/
    
    //strlen()==>문자열 길이 반환
    //strcmp()==>문자열 비교
    //strcpy()==>문자열 복사

    //문자열이 숫자를 출력할수 있게 하기
    /*char s[30];
    int i;
    gets(s);

    i = atoi(s);
    printf("%d", i);*/

    //구조체 &&백준 문자열 풀기
    return 0;
}
